type User {
  id: ID! @unique
  name: String!
  email: String!
  country: String!
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  image: String
  largerImage: String
  price: Int!
  # createAt: DateTime!
  # updatedAt: DateTime!
  # user: User!
}
